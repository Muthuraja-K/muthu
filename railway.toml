[build]
builder = "nixpacks"

[deploy]
startCommand = "python3.11 -m uvicorn main:app --host 0.0.0.0 --port $PORT --log-level info"

[env]
SECRET_KEY = "your-production-secret-key-change-this-in-railway-dashboard"
ALLOWED_ORIGINS = "*"